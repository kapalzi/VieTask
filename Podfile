# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'VieTask' do
  use_frameworks!
  pod 'BlueSocket'
  pod 'RxSwift', '~> 5'
  pod 'RxCocoa', '~> 5'
end

target 'VieTaskTests' do
  pod 'Quick'
  pod 'Nimble'
end
